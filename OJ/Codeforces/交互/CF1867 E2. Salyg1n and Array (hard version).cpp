#include<bits/stdc++.h>
using namespace std;
const int maxn = 1e5 + 10;
/*
 *
 */
int main()
{
	int t, n, k, ans = 0;
	std::ios::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	cin >> t >> n >> k;
	while (t--)
	{
		cout << "";
		cout.flush();
		cin >> 
	}
	cout << "! " << ans << endl;
	return 0;
}
